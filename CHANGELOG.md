# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.4.0] - 2026-09-18

女仆不用再跑回仓库了：给她一台**无线女仆终端**，她站在工地就能从 ME 网络取料。

### 新增

- **无线女仆终端**：女仆饰品形态的 ME 终端
  - 放进女仆的**饰品栏或背包**，她缺料时直接从网络取，不必再跑回仓库那边；
    取料与还料都在原地完成，她不会因为带着它多走一步路
  - 链接方式与 AE2 官方无线终端一致：把终端放进 **ME 无线访问点**的槽位里链接
  - 右键空气打开 AE 原版终端界面；Shift + 右键空气断开链接
  - 判定与提示全部沿用官方：没链接提示「设备未链接」，没电提示「设备未通电」
  - 和官方一样，刚合成的终端是空电的，先在充能器里充一次电即可
- **女仆绑定卡**：插进终端的升级槽之后
  - 终端的耗电改由 **AE 网络**承担，内置电池不再流失
  - **跨维度**时耗电固定为 **22.5 AE/tick**（取值参照 AE2WTLib 的量子桥卡）。
    不插卡时 AE2 算不出跨维度的距离，给出的速率是 1e154 量级——一 tick 就能把
    整张网络抽干，跨维度实际上没法用
  - 女仆带着它取料时，也不再看距离与维度
- 拿着终端**右键方块**时照旧交给方块：右键充能器能放进去充电，
  右键无线访问点能开访问点的界面

### 文档

- `DEVELOPER.md` §7.11 记下这一轮的教训：能覆写的只有**取电、跨维度耗电、断开链接**，
  `getLinkedGrid`（「设备未链接」的提示就写在它里面）、`checkPreconditions`、
  射程判定一律照抄官方——自己接管判定的结果是"右键没反应，且没有任何提示"
- `README.md` 补上无线女仆终端与绑定卡的用法、距离与电量规则

## [1.3.1] - 2026-09-17

### 修复

- **挖掉女仆终端什么都不掉**：这个方块一直没有战利品表，破坏后直接凭空消失、
  不返还任何东西——而它是能合成的（`ae2:terminal` + 半暗监视器 + 线缆接口），
  挖一次就白搭一套材料。现在和创造女仆接口一样，会掉回自身
- **挖掉终端会丢掉里面的升级卡**：战利品表补上了方块实体数据的转移，
  重新放下时升级槽里的卡还在（女仆终端和创造女仆接口都已处理）

## [1.3.0] - 2026-09-17

这一版是两件新东西：**模组专属的创造物品栏**，以及一个**取之不尽的物料来源**。

### 新增

- **创造物品栏**：蓝图、绑定书、女仆终端不再混在原版的「工具与实用物品」里翻找，
  模组的物品单独占一栏
- **创造女仆接口**：白色发光的方块，一个取之不尽的物料来源
  - 用绑定书指过去，女仆施工时缺什么拿什么，**不需要 AE 网络，也不需要通电**
  - 贴着线缆放置，则把这份库存挂进 ME 网络，**任意终端里都能取到游戏中的所有物品**
  - 右键打开 AE2 官方终端界面；没接网络时显示的就是它自己的创造库存，永远不是空的
  - 女仆用不完的剩料会被它收下清掉，不再堆在女仆身上；
    但它**不会**吞掉你存进 ME 网络的东西

### 改进

- **署名与协议**：作者署名改为 伊纳（lyina）；补上 MIT `LICENSE`，
  并随 jar 分发到 `META-INF/LICENSE`（MIT 要求协议随所有副本分发）
- 工程名统一为 `MaidConstruction` —— jar 清单的 `Implementation-Title`
  此前取的是目录名 `MyMod`

### 文档

- `DEVELOPER.md` 补上创造女仆接口的设计说明：两个存储出口为什么不能合并、
  存储挂载必须早于节点创建、以及"发光"为什么要靠模型面级全亮
- `README.md` 补上创造女仆接口的用法与限制

## [1.2.0] - 2026-09-17

这一版集中在两件事上：**材料的判定方式**，以及**结构旋转时的朝向**。

材料这块原来是拿 `Block.asItem()` 一刀切的，对 AE2 的线缆完全不成立——那是个"容器方块"，
一个格子里的东西由若干个部件拼成。现在改成按方块的实际内容去算，并支持一个方块需要多样材料。

朝向这块原来只转了方块状态，而有一批方块（AE2 的机器）根本没覆写旋转逻辑，
另有一批（线缆部件）的朝向压根不在状态里。现在两条路都补上了。

### 新增

- **缺料清单**：女仆找不到材料时，直接列出缺哪些物品、各缺多少（最多 5 种，更多时补一句"等共 N 种"）。
  物品名按客户端语言显示，不会固定在服务端的语言上
- **线缆轮廓**：蓝图投影和面板预览里，ME 线缆现在画成"芯 + 连接臂 + 部件标记"，
  终端是绿色、线缆是青色、其余部件是琥珀色。此前这些方块在预览里**完全不可见**
- **材料解析扩展点**：新增 `BlockMaterials` 注册链，让各模组自行说明"建造这个方块需要什么"，
  核心逻辑不必知道 AE2 的存在

### 改进

- **AE2 线缆按部件算材料**：`ae2:cable_bus` 只是个壳，玩家实际放上去的是贴在各面的部件
  （线缆本体、终端、存储总线……）。它的 `asItem()` 返回的是一个游戏里拿不到的方块物品，
  于是女仆抱着一个错误的物品名去箱子里找，箱子里明明躺着线缆和终端也视而不见。
  现在从部件 NBT 的 `id` 字段还原真实的物品
- **取料按缺口，不再按总量**：新增"从现在这个进度出发还差多少"的计算，会跳过工地上已经建好的方块。
  此前每缺一次料都会把整座建筑的量重新搬一遍，手上的材料越堆越多
- **取料上限按背包实际空位**：此前硬编码"一趟最多搬 8 种材料"，建筑用超过 8 种就永远凑不齐；
  装了背包升级也不会多拿
- **绑定书与蓝图优先于容器**：原来的交互顺序是方块优先，右键容器或终端时会被打开界面打断，
  根本轮不到绑定/选点。现在这两样物品会抢在方块之前处理
- **空手而归的提示分成了两种**："背包塞不下"和"这里没有还缺的材料"。
  此前一律提示"检查一下背包是不是满了"，问题不在背包时等于误导
- **缺料提示只播报一次**：同一批缺料不再反复刷屏，等清单变化（又建了一部分、换了蓝图）才重新提醒

### 修复

- **结构旋转后机器朝向不变**：`BlockState.rotate()` 会委托给 `Block.rotate()`，
  而后者**默认直接返回原状态**——只有楼梯、箱子这类主动覆写过的方块才会真的转。
  AE2 的机器（磁盘驱动器、接口等）继承的是它自己的基类，没覆写，于是朝向纹丝不动。
  现在蓝图会在方块自己转完之后，按原始值把所有朝向属性重设一遍
- **结构旋转后线缆部件朝向不变**：部件挂在哪个面记在 NBT 的**键名**上，
  方块状态旋转带不动它，需要单独把这些键挪到旋转后的方向上
- **旋转蓝图后投影崩溃**：投影缓存的方块坐标来自旋转前的结构，而结构旋转后宽长互换，
  坐标越界导致渲染线程抛异常、游戏直接退出

## [1.1.1] - 2026-09-17

### 改进

- **绑定书的高亮现在能穿透方块和生物。** 之前用的是带深度测试的线框，目标被墙挡住就
  完全看不见——而这恰恰是高亮最该发挥作用的时候（找仓库深处的箱子）。现在改成手动
  构建顶点、直接控制深度状态，线宽也从 1px 加粗到 2.5px，站在基地里就能看见目标。

### 文档

- 修正 README 里蓝图面板的描述：补上**名称输入框**、**打开文件夹**按钮和**状态提示行**
- 修正导入导出的说明：实际扩展名是 `.blueprint`（不是 `.blueprint.nbt`），
  文件名**跟随蓝图名**且同名覆盖（不是带时间戳），另外有 1 MB 的单文件上限

## [1.1.0] - 2026-09-17

首个带 AE2 支持的版本。

### 新增

- **绑定书**：右键容器绑定，可以放进女仆的饰品栏。绑的是坐标 + 维度而不是容器实体，
  箱子拆了重放照样能用；工具提示显示绑定处的方块名称与坐标
- **两级取材**：女仆优先搜身边 10 格，附近实在没有才照绑定书跑一趟远处的仓库
- **AE2 取料**：绑定书指向 ME 网络的任意接入点都能用——存储总线、ME 接口、终端、
  驱动器、控制器、线缆。提取前会先模拟确认背包容量，不会出现"提出来又塞不回去"导致物品蒸发
- **女仆终端方块**：六个面都是终端面板，右键打开的是 AE2 原版终端界面
  （物品列表、搜索、排序、合成全部可用）；同时把整个网络作为标准存储能力暴露出去，
  因此绑定书也能直接把它当容器用

### 改进

- **按需取料**：结构只要 5 块石头就只拿 5 块，不再无脑掏一整组
- **完工后归还剩料**：送回容器时优先选绑定书指定的那个；容器满了或附近没有容器，
  材料会留在女仆身上，绝不丢弃
- **完工后收走蓝图**：建完的蓝图自动从手上收进背包，女仆随即会挑出背包里
  下一张还没建完的图接着干——一次塞好几张，她一张张建完
- **已完工的蓝图不再前往工地**，避免工地方块被拆掉几块后女仆反复来回跑
- 取料时材料不再落进女仆的装备栏（改用纯背包视图，不再往手部/盔甲槽塞东西）

### 修复

- **女仆的待命状态不再被永久改变**：施工前的原始状态改存在女仆自己的持久数据里，
  不再随控制器实例重建而丢失
- 归还流程加了超时，避免女仆卡在"施工中"收不了工
- 取料的到达判定只看水平距离，容器比女仆高一格或低一格也能正常交互
- 导航目标改为容器**旁边的落脚点**，女仆不再贴着箱子蹭甚至往上跳
- 绑定书现在能抢到方块的右键，不再被容器抢先打开界面导致绑定失败
- 女仆终端补齐了 `IActionHost`，界面不再是一片空白
