# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.5.6] - 2026-09-19

修一处**能把客户端直接崩掉**的连带问题：整合包里注册表被第三方动过时，打开创造物品栏会崩。

### 修复

- 修：**打开创造物品栏时客户端崩溃**
  （`NullPointerException: Registry Object not present: blueprint:wireless_maid_terminal`）。
  创造栏摆放物品时对 AE2 那几件直接调了 `.get()`，而整合包里真会出现"同一批注册项里
  有的在、有的不在"——一旦缺了谁，这句 `.get()` 就把异常抛进
  `CreativeModeInventoryScreen` 的构造里，**玩家一开创造栏就崩**，报错还落在本模组头上
  （JEI 构建物品列表时也会踩到同一个坑，只是被它自己吞掉了）。
  现在**一件一件先问"在不在"再摆**：缺的跳过、创造栏照常打开，并留一行日志说明缺了哪些
- 启动阶段的升级卡关联、无线访问点链接登记同样先判存：缺了就跳过并说明，
  而不是在启动时抛一句让人看不懂的 NPE
- 对外取"无线女仆终端 / 女仆绑定卡"改用 `orElse(null)`：这两件本来就按可空处理，
  不该因为注册项缺失就抛异常

### 说明

- 从玩家日志看，那几件在**启动时还都在**（升级卡关联那一步取到过），是之后才查不到的——
  属于整合包里第三方改写注册表的范畴，不是本模组自己丢注册项。这一版把"对方缺了"
  从"客户端崩溃"降级成"少一件物品 + 一行日志"

## [1.5.5] - 2026-09-19

施工不再"吃掉"地上的东西：被目标方块顶掉的方块会拆下来收好。

### 新增

- **顶掉的东西一律回收**：建造是"在这个位置放成这个方块"，位置上原本的草、树、墙被替换时，
  以前是 `setBlock` 直接覆盖——原方块连着掉落物一起消失。现在照它的战利品表拆下来，
  去处依次是**她自己的背包 → 饰品栏里的无线终端 → 脚边地上**，一件都不会凭空没了
  - **容器里的东西一并掏出来**：拿箱子当建材也不怕，里面的东西照同样的顺序收好
  - 掉落按**她那套默认工具**算：**下界合金等级**，看方块类型配镐 / 斧 / 锹 / 锄，没有附魔、没有剪刀
- **拆不动会说**：要她没有的工具、或者**压根谁也拆不掉**的方块，她会说一句
  「%s 我拆不动——得有对应的工具才挖得开，这块我先留着」。同一种方块只说一次

### 修复

- 修：**基岩会被顶掉**。她以前把"放方块"和"挪开挡路的东西"当成一回事——直接 `setBlock` 覆盖，
  于是连基岩、屏障这类**谁也拆不掉**的方块都能被她删掉（删了就回不来了）。
  现在这类方块她**一律不碰**：位置原样留着，算进完工时那句「还有几块放不下」，
  同时说一声「%s 我动不了——那不是能挖掉的东西」。**不可破坏**的判据用的是原版那条
  （"挖掉它要多久"小于零），所以模组里声明为不可破坏的方块也一并管住
- 修：需要**更高级工具**的方块同样不再硬盖（以前会盖掉且收不到东西）。她动不了就留着，
  主人补上工具或者自己动手；这条和上面那条分开说话，因为一个是"给我工具"，一个是"本来就拿不走"

### 修复

- 修：**缺料的同一句话反复刷屏**。四种说法（找不到来源、容器里没有、背包塞不下、绑定书仓库空了）
  共用**一格**「说过了」的指纹，互相覆盖——"容器里没有"刚写下指纹，"找不到来源"就把它顶掉，
  转回来又成了"新内容"。两句话来回覆盖，屏幕上就成了无限复读，也正是 1.5.4 那次去重对它无效的原因。
  现在**每种说法各自记账**，另外两次开口之间留 3 秒（同一次尝试里好几条路都想说话时只放第一条出去）；
  缺的东西补齐后销账，所以下次真再缺时她还会开口

### 说明

- 她**不会为了某一块去翻工具**（剪刀、剑这类不在她的行李里）：草和树叶要剪刀才有收成，
  她就空着手拆，收不到什么属于预期行为——想要那些东西请自己动手
- **"模组给了工具才能破的方块"怎么办**（比如某把镐能破基岩，而方块本身还是不可破坏的）：
  她的判据是"这一格当下在这个世界里硬不硬"，**不看方块名字**——模组把基岩换成自己能挖的那种、
  或者改了它的硬度，她立刻就能拆；但模组只是给**工具**加特技时，她不会去用那件工具，
  于是方块留着不动（说一声、算进"还剩几块放不下"）。**主人自己敲掉**那一块之后，
  下一轮重扫她就照蓝图补上。想让这类方块也归她拆，把方块加进标签
  **`blueprint:maid_clearable`**（默认空，数据包里 `data/blueprint/tags/blocks/maid_clearable.json` 一行即可），
  进了标签就不再过问硬度

## [1.5.4] - 2026-09-19

女仆施工看得见进度：站在她旁边就知道她建到哪了。顺带修掉两处"账记错了"的地方。

### 新增

- **施工进度条**：站在女仆 **16 格内**时，准星下方显示她这一单的进度（她的名字 + 还剩几块 + 百分比）。
  同时有几只在建只显示最近那一只；走远、停工都会自己消失

### 改进

- **建全没建全说得清**：有方块因为缺支撑、位置被占而最终放不下时，她不再含糊地报「建好啦」，
  而是写明还剩几块（这个数字其实一直在算，只是没人说出口）
- **蓝图 tooltip 对上了实际操作**：「Shift + 右键空气取消定位」早改成了「打开蓝图面板」，文案一直没跟上
- 修：**进度条每隔几秒掉回 0 再涨回来**——定期重扫会重建施工会话，而新会话的游标从 0 开始，
  已经建好的那几百块被算成"还没建"。现在会话开局会照工地现状把游标先推到第一块缺口处
- 修：**缺料提示反复弹**——找不到来源、来源里没有、背包塞不下这三条路径各记各的账，
  30 秒冷却一过又是一条。现在统一按"内容指纹"去重：同一批缺料只说一次，清单真变了才再说

### 说明

- 进度是服务端推给**附近**玩家的新网络包，协议版本从 2 升到 **3**：
  服务器与客户端**两边都要换成 1.5.4**，混装会连不上（这是刻意的，好过在游戏里出怪事）

## [1.5.3] - 2026-09-19

学习池从"看"变成"用"：界面里每个图标都能点着走，下单则自动进「工业模式」干活。

### 新增

- **工业模式**：照学习池里下的单开工的工作模式
  - **下单会自动切过去**，做完最后一单再**自动切回下单前的模式**（比如学习模式），不用手动拨
  - **只在工作时间干活**：作息沿用**车万女仆自己那套**（白天 / 夜晚 / 全天）。休息时段她不动工，
    单子排队等着，到点自己开工
- **学习池里可以一路点下去**：
  - 摆法里的**材料格**：左键跳过去看它的做法（她没学过就当场说一声），右键看它能拿来做什么
  - 摆法底下的**产物格**：右键看它的用途
  - **用途页里已列出的图标**：左键跳过去，右键继续看**它自己**的用途
- **她的话改成界面内飘字**：忘掉、换做法、下单失败、没学会——都在界面上飘一句。
  之前走聊天栏 / 动作栏，而**开着界面时那一层根本不画**，所以"点了没反应"
- **材料循环单独提示**：A 要用 B、B 又要用回 A 时说的是「绕成死循环了」，
  而不是含糊的"还缺材料"（那会把人支去再塞点材料，可这条路塞多少都走不通）
- **上下班有自己的话**（只在工业模式下）：
  - **下班**那一刻她会高兴地说一句「下班啦～」，手上还有单就补一句「剩下的活上班时间接着做」，
    并且开心一下；刚进游戏 / 刚切模式时只记不报，不会莫名其妙"下班"一次
  - 还没到上班时间而手上有单时，她会说「现在不是我的上班时间，到点我就开始做」——
    **同一张单只说一次**（这句走聊天栏，每分钟念一遍是噪音），
    面板左下同时写一行「（她在休息：还没到上班时间）」，主人不必猜她为什么不动

### 改进

- **下单不再播报**：成功不吭声（主人刚点的按钮，回声是噪音），切模式也不播报
- **提示统一成她的口吻**：「名字：……」——施工那 17 条（"女仆找不到材料"）一并改成第一人称
  （"我找不到建造要用的材料"）
- **下单能不能排上，界面先算**：排不上就当场说，不再发一个注定被服务端拒掉的包
- **她的话不会被别的提示压住**：与她那句话重叠的悬停提示会自动让开（只让重叠的那一个）
- 底部那行操作指南收进左下角的**「?」**，鼠标停上去才展开——平铺永远嫌长，换个语言更长
- **摆法补上「卡片 + 槽位」**：材料格 → 箭头 → 产物 圈成一张卡，3×3 每格都铺底描框
  （空槽也画）。之前物品直接落在近乎全黑的面板上，看着像悬在墨水里，
  也看不出"这里有九格、哪一格是空的"

### 移除

- **学习池的「停用 / 启用」撤掉了**（Shift + 右键那条）：右键改成了「看用途」。
  不想让她做某样东西时，直接**忘掉**即可——再演示一次就补回来了

## [1.5.2] - 2026-09-19

手搓这条线收尾：缺料提示不再绕圈子，做好东西也不用惦记着从她背包里掏。

### 新增

- **做好直接进终端**：她饰品栏里带着**无线女仆终端**时，手搓出来的成品（连同余料，比如空桶）
  不再堆在她背包里，而是直接送进终端连的 ME 网络，就地完成、不用走动
  - 网络满了、或者终端刚好失效也没关系：东西会**原样留在她背包里**，不会凭空消失，你还能从她身上拿

### 改进

- **缺料提示改成"你要的那样东西的直接用料"**：做木剑缺木板和木棍，现在报的就是
  「做 木剑 还缺材料：木板 ×2、木棍 ×1」
  - 之前只报**排头那张单**：嵌套合成会把零件单插到最前面，于是你看到的永远是零件的缺口
    （「做木棍 还缺材料：木板 ×2」）；料只够做零件时，做完零件又冒一条一模一样的，像是永远填不满
  - 零件的料不用你另外凑：她做零件要用的那份，到了容器跟前自己会拿
  - 数量按**剩余单数**放大：一次下单 ×3 就一次报全，不用做完一个再报一回

### 修复

- **一个零件插不进去，整条嵌套就全放弃**：`tryNest` 找到第一个能做的零件后直接
  `return insertFirst(...)`，插不进去（那号产物已经有单在排、绕回了来路、队满）就返回失败，
  剩下的缺口连试都不试、直接报"缺料"。现在插不进去会换下一样接着试

## [1.5.1] - 2026-09-18

学习池从"只进不出"变成**管得住**：哪样先别做、哪种做法别用，都能就地按下暂停。

### 新增

- **给学习池按暂停**：**Shift + 右键**产物 = 整样停用 / 再按一下启用
  - 停用的她**照样记得怎么做**，只是不再接新的单；**已经挂着的单照常做完**，不会半途扔下
  - 停用时下单按钮变灰，池子里那格压一层灰，一眼看得出哪些她当下不做
- **也能单独停用某一条做法**：**Shift + 右键**右边列出的那条配方
  - 停用后她自动改照**下一条启用的**做法做，界面上「优先」那个标记也跟着挪过去
  - 全停用了才等于"这东西暂时做不了"
- **学习池可以删除**：**Shift + 左键**产物 = 忘掉（连同它所有做法）
- **用途页改成物品图标**网格 + 滚轮翻页：原来是一行行中文名，面板窄到基本必被截断，
  现在一格一样产物，跟左边那片格子一个规矩

### 改进

- **用途页改为只列「她已学会的」**：原来是扫配方表，木板这类能有几百条，只能靠上限
  砍到 32 条——这就是"缺失特别严重"的来由。现在反过来查学习池：**她会做的、用到这个的
  东西**，每一条都点得过去、都能直接下单，不必再砍条数
- **学习池不再设容量上限**：原来满 512 条后就静默不再记新的
- 底部操作指南不再超出屏幕（文案缩短，并按窗口宽度兜底截断）

### 修复

- **演示时记下来的配方摆法会丢**：录入时存的是合成格里那件物品**本身**、不是副本，
  下一步"逐格扣减材料"时摆法跟着一起被扣空。表现是刚学会的做法，摆法那一片过后全空、
  只剩配方 id；id 也没认出来的那些整条做法等于没记上
- **单人游戏打开学习池直接闪退**：`InteractMaidEvent` 在集成服务端线程上也会走一遍，
  而开界面那句只按**物理端**（单人游戏的物理端就是 CLIENT）判断、认不出线程，于是服务端
  线程也跑去开界面，撞上 `Rendersystem called from wrong thread`。专用服务端不受影响
- 用途页底部两行重叠：翻页提示与"点一下跳过去"叠在同一行

## [1.5.0] - 2026-09-18

让女仆**学会怎么做**，然后照着你定的做法替你把东西做出来。

### 新增

- **学习模式**：女仆跟在主人身边，把主人**亲手做出来**的东西记进她的「学习池」
  - 工作台 3×3、背包 2×2、**ME 合成终端**里的合成都算数（得在她附近 8 格内）
  - 学会一样（或学会同一样东西的**新做法**）时她会高兴地叫两声——用她自己的声音包，不放别的音效
- **学习池界面**：**蹲下 + 空手右键自己的女仆**打开
  - 左边一格一样**产物**（不铺配方：一样东西可能有好多做法，平铺出来全是重复图标），
    配方多于一个的会**描金框**并标上数量
  - **搜索栏**：按名字或物品 id 过滤（搜「火把」、`torch`、`ae2:` 都行）
  - **左键**产物看她的配方，**右键**看这东西**能用来做什么**（点一条跳到那个产物）
  - 一样产物可以有好几个配方，**第 1 条就是她会照做的那个**；点一条配方就把它排到最前
- **下单让她做**：选中产物 → 填数量（1–256）→ 下单
  - 她照**那条优先配方**手搓；材料就近容器 → 绑定书指定的仓库 → 身上无线女仆终端（AE2，就地取）
  - **会嵌套**：缺的零件只要她自己会做，就先去做零件（要木棍而没木板 → 先做木板），
    能套多深就套多深，只挡"绕回自己"那种循环配方；排队行会写成「木棍 ×4（为了做 火把）」
  - **做完了跑回来告诉你**：她先把手头的活干完，再走到你身边说一句「主人，做好了：X」
  - 缺料会在聊天栏提醒一次，并分得清"仓库里没有"和"背包塞不下"；做不了的单会撤掉并说明
  - 一样产物只有一张单，重复下单是**加数量**

### 修复

- **学习池的数据从来没登记进女仆模组**：`registerTaskData` 漏了，数据写进去了但读回来永远
  是 null——表现是"学过全不记得"，而且**不报任何错**。新增 `TaskDataKey` 时必须走那个回调
- **ME 合成终端里学到的是"认不出的配方"**：AE2 传进合成事件的容器是 `InternalInventory`
  包装出来的适配器，不是 `CraftingContainer`，按原版容器的读法什么也读不到。
  现在改走 `CraftingTermMenu#getCurrentRecipe()`（无线、便携合成终端一并覆盖）
- **认不出配方时不再塞一条假配方**：只记产物，界面会说「只见过产物，没见过做法（再演示一次她就记住了）」
- **绑定书指定的仓库取不到料**：学习模式每 tick 的"跟上去"会把取料导航顶掉，她永远走不到远程仓库
  ——附近的箱子几步就到，所以这个毛病只在远程仓库上才露头。规则改为**手上有单就不跟人**
- 提示不再写错线索：搬运回来是 0 时，先说清是"仓库里没有"还是"她背包塞不下"

### 文档

- `README.md` 新增「学习模式与学习池」：交互表、按钮变灰规则、下单与嵌套、做到哪一步会说
- `DEVELOPER.md` 新增 §7.12（学习池：展示按产物、存储按配方）、§7.13（下单与手搓）：
  含"问合成自己而不是猜界面类型"、"挡循环靠来路不靠层数"、"多套导航必须写明让位条件"、
  "缺料与背包塞不下要分开说"等这一轮的坑

## [1.4.0] - 2026-09-18

女仆不用再跑回仓库了：给她一台**无线女仆终端**，她站在工地就能从 ME 网络取料。

### 新增

- **无线女仆终端**：女仆饰品形态的 ME 终端
  - 放进女仆的**饰品栏或背包**，她缺料时直接从网络取，不必再跑回仓库那边；
    取料与还料都在原地完成，她不会因为带着它多走一步路
  - 链接方式与 AE2 官方无线终端一致：把终端放进 **ME 无线访问点**的槽位里链接
  - 右键空气打开 AE 原版终端界面；Shift + 右键空气断开链接
  - 判定与提示全部沿用官方：没链接提示「设备未链接」，没电提示「设备未通电」
  - 和官方一样，刚合成的终端是空电的，先在充能器里充一次电即可
- **女仆绑定卡**：插进终端的升级槽之后
  - 终端的耗电改由 **AE 网络**承担，内置电池不再流失
  - **跨维度**时耗电固定为 **22.5 AE/tick**（取值参照 AE2WTLib 的量子桥卡）。
    不插卡时 AE2 算不出跨维度的距离，给出的速率是 1e154 量级——一 tick 就能把
    整张网络抽干，跨维度实际上没法用
  - 女仆带着它取料时，也不再看距离与维度
- 拿着终端**右键方块**时照旧交给方块：右键充能器能放进去充电，
  右键无线访问点能开访问点的界面

### 文档

- `DEVELOPER.md` §7.11 记下这一轮的教训：能覆写的只有**取电、跨维度耗电、断开链接**，
  `getLinkedGrid`（「设备未链接」的提示就写在它里面）、`checkPreconditions`、
  射程判定一律照抄官方——自己接管判定的结果是"右键没反应，且没有任何提示"
- `README.md` 补上无线女仆终端与绑定卡的用法、距离与电量规则

## [1.3.1] - 2026-09-17

### 修复

- **挖掉女仆终端什么都不掉**：这个方块一直没有战利品表，破坏后直接凭空消失、
  不返还任何东西——而它是能合成的（`ae2:terminal` + 半暗监视器 + 线缆接口），
  挖一次就白搭一套材料。现在和创造女仆接口一样，会掉回自身
- **挖掉终端会丢掉里面的升级卡**：战利品表补上了方块实体数据的转移，
  重新放下时升级槽里的卡还在（女仆终端和创造女仆接口都已处理）

## [1.3.0] - 2026-09-17

这一版是两件新东西：**模组专属的创造物品栏**，以及一个**取之不尽的物料来源**。

### 新增

- **创造物品栏**：蓝图、绑定书、女仆终端不再混在原版的「工具与实用物品」里翻找，
  模组的物品单独占一栏
- **创造女仆接口**：白色发光的方块，一个取之不尽的物料来源
  - 用绑定书指过去，女仆施工时缺什么拿什么，**不需要 AE 网络，也不需要通电**
  - 贴着线缆放置，则把这份库存挂进 ME 网络，**任意终端里都能取到游戏中的所有物品**
  - 右键打开 AE2 官方终端界面；没接网络时显示的就是它自己的创造库存，永远不是空的
  - 女仆用不完的剩料会被它收下清掉，不再堆在女仆身上；
    但它**不会**吞掉你存进 ME 网络的东西

### 改进

- **署名与协议**：作者署名改为 伊纳（lyina）；补上 MIT `LICENSE`，
  并随 jar 分发到 `META-INF/LICENSE`（MIT 要求协议随所有副本分发）
- 工程名统一为 `MaidConstruction` —— jar 清单的 `Implementation-Title`
  此前取的是目录名 `MyMod`

### 文档

- `DEVELOPER.md` 补上创造女仆接口的设计说明：两个存储出口为什么不能合并、
  存储挂载必须早于节点创建、以及"发光"为什么要靠模型面级全亮
- `README.md` 补上创造女仆接口的用法与限制

## [1.2.0] - 2026-09-17

这一版集中在两件事上：**材料的判定方式**，以及**结构旋转时的朝向**。

材料这块原来是拿 `Block.asItem()` 一刀切的，对 AE2 的线缆完全不成立——那是个"容器方块"，
一个格子里的东西由若干个部件拼成。现在改成按方块的实际内容去算，并支持一个方块需要多样材料。

朝向这块原来只转了方块状态，而有一批方块（AE2 的机器）根本没覆写旋转逻辑，
另有一批（线缆部件）的朝向压根不在状态里。现在两条路都补上了。

### 新增

- **缺料清单**：女仆找不到材料时，直接列出缺哪些物品、各缺多少（最多 5 种，更多时补一句"等共 N 种"）。
  物品名按客户端语言显示，不会固定在服务端的语言上
- **线缆轮廓**：蓝图投影和面板预览里，ME 线缆现在画成"芯 + 连接臂 + 部件标记"，
  终端是绿色、线缆是青色、其余部件是琥珀色。此前这些方块在预览里**完全不可见**
- **材料解析扩展点**：新增 `BlockMaterials` 注册链，让各模组自行说明"建造这个方块需要什么"，
  核心逻辑不必知道 AE2 的存在

### 改进

- **AE2 线缆按部件算材料**：`ae2:cable_bus` 只是个壳，玩家实际放上去的是贴在各面的部件
  （线缆本体、终端、存储总线……）。它的 `asItem()` 返回的是一个游戏里拿不到的方块物品，
  于是女仆抱着一个错误的物品名去箱子里找，箱子里明明躺着线缆和终端也视而不见。
  现在从部件 NBT 的 `id` 字段还原真实的物品
- **取料按缺口，不再按总量**：新增"从现在这个进度出发还差多少"的计算，会跳过工地上已经建好的方块。
  此前每缺一次料都会把整座建筑的量重新搬一遍，手上的材料越堆越多
- **取料上限按背包实际空位**：此前硬编码"一趟最多搬 8 种材料"，建筑用超过 8 种就永远凑不齐；
  装了背包升级也不会多拿
- **绑定书与蓝图优先于容器**：原来的交互顺序是方块优先，右键容器或终端时会被打开界面打断，
  根本轮不到绑定/选点。现在这两样物品会抢在方块之前处理
- **空手而归的提示分成了两种**："背包塞不下"和"这里没有还缺的材料"。
  此前一律提示"检查一下背包是不是满了"，问题不在背包时等于误导
- **缺料提示只播报一次**：同一批缺料不再反复刷屏，等清单变化（又建了一部分、换了蓝图）才重新提醒

### 修复

- **结构旋转后机器朝向不变**：`BlockState.rotate()` 会委托给 `Block.rotate()`，
  而后者**默认直接返回原状态**——只有楼梯、箱子这类主动覆写过的方块才会真的转。
  AE2 的机器（磁盘驱动器、接口等）继承的是它自己的基类，没覆写，于是朝向纹丝不动。
  现在蓝图会在方块自己转完之后，按原始值把所有朝向属性重设一遍
- **结构旋转后线缆部件朝向不变**：部件挂在哪个面记在 NBT 的**键名**上，
  方块状态旋转带不动它，需要单独把这些键挪到旋转后的方向上
- **旋转蓝图后投影崩溃**：投影缓存的方块坐标来自旋转前的结构，而结构旋转后宽长互换，
  坐标越界导致渲染线程抛异常、游戏直接退出

## [1.1.1] - 2026-09-17

### 改进

- **绑定书的高亮现在能穿透方块和生物。** 之前用的是带深度测试的线框，目标被墙挡住就
  完全看不见——而这恰恰是高亮最该发挥作用的时候（找仓库深处的箱子）。现在改成手动
  构建顶点、直接控制深度状态，线宽也从 1px 加粗到 2.5px，站在基地里就能看见目标。

### 文档

- 修正 README 里蓝图面板的描述：补上**名称输入框**、**打开文件夹**按钮和**状态提示行**
- 修正导入导出的说明：实际扩展名是 `.blueprint`（不是 `.blueprint.nbt`），
  文件名**跟随蓝图名**且同名覆盖（不是带时间戳），另外有 1 MB 的单文件上限

## [1.1.0] - 2026-09-17

首个带 AE2 支持的版本。

### 新增

- **绑定书**：右键容器绑定，可以放进女仆的饰品栏。绑的是坐标 + 维度而不是容器实体，
  箱子拆了重放照样能用；工具提示显示绑定处的方块名称与坐标
- **两级取材**：女仆优先搜身边 10 格，附近实在没有才照绑定书跑一趟远处的仓库
- **AE2 取料**：绑定书指向 ME 网络的任意接入点都能用——存储总线、ME 接口、终端、
  驱动器、控制器、线缆。提取前会先模拟确认背包容量，不会出现"提出来又塞不回去"导致物品蒸发
- **女仆终端方块**：六个面都是终端面板，右键打开的是 AE2 原版终端界面
  （物品列表、搜索、排序、合成全部可用）；同时把整个网络作为标准存储能力暴露出去，
  因此绑定书也能直接把它当容器用

### 改进

- **按需取料**：结构只要 5 块石头就只拿 5 块，不再无脑掏一整组
- **完工后归还剩料**：送回容器时优先选绑定书指定的那个；容器满了或附近没有容器，
  材料会留在女仆身上，绝不丢弃
- **完工后收走蓝图**：建完的蓝图自动从手上收进背包，女仆随即会挑出背包里
  下一张还没建完的图接着干——一次塞好几张，她一张张建完
- **已完工的蓝图不再前往工地**，避免工地方块被拆掉几块后女仆反复来回跑
- 取料时材料不再落进女仆的装备栏（改用纯背包视图，不再往手部/盔甲槽塞东西）

### 修复

- **女仆的待命状态不再被永久改变**：施工前的原始状态改存在女仆自己的持久数据里，
  不再随控制器实例重建而丢失
- 归还流程加了超时，避免女仆卡在"施工中"收不了工
- 取料的到达判定只看水平距离，容器比女仆高一格或低一格也能正常交互
- 导航目标改为容器**旁边的落脚点**，女仆不再贴着箱子蹭甚至往上跳
- 绑定书现在能抢到方块的右键，不再被容器抢先打开界面导致绑定失败
- 女仆终端补齐了 `IActionHost`，界面不再是一片空白
